{% block captcha_row %}

    <puzzle-captcha
        {%- for attrname, attrvalue in attr -%}
        
            {# Affiche chaque attribut et sa valeur sous la forme `attribut="valeur"`, échappant le HTML brut #}
            {{- (" " ~ attrname ~ "=\"" ~ attrvalue ~ "\"") | raw('html')}}
        {%- endfor -%}
        >
        {{ form_widget(form.challenge, {value: challenge}) }}
        {{ form_widget(form.answer) }}
    </puzzle-captcha>
{{ form_help(form) }}
{{ form_errors(form) }}

{% endblock %}