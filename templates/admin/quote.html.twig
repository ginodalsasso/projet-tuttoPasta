<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Quote</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .quote {
            max-width: 600px;
            margin: 0 auto;
        }
        .header, .footer {
            text-align: center;
        }
        .content {
            margin: 20px 0;
        }
        .content table {
            width: 100%;
            border-collapse: collapse;
        }
        .content table, .content th, .content td {
            border: 1px solid black;
        }
        .content th, .content td {
            padding: 8px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="quote">
        <div class="header">
            <h1>Quote Reference: {{ quote.reference }}</h1>
            <p>Date: {{ quote.quoteDate|date('Y-m-d H:i') }}</p>
        </div>
        <div class="content">
            <h2>Customer Information</h2>
            <p>Name: {{ quote.customerFirstName }} {{ quote.customerName }}</p>
            <p>Email: {{ quote.customerEmail }}</p>
            <h2>Appointment Information</h2>
            <p>Appointment ID: {{ appointment.id }}</p>
            <p>Start Date: {{ appointment.startDate|date('Y-m-d H:i') }}</p>
            <p>End Date: {{ appointment.endDate|date('Y-m-d H:i') }}</p>
            <!-- Ajoutez d'autres informations pertinentes ici -->
        </div>
        <div class="footer">
            <p>&copy; {{ "now"|date("Y") }} Your Company</p>
        </div>
    </div>
</body>
</html>
