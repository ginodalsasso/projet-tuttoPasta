{% extends 'base.html.twig' %}

{% block head %}
    {{ parent() }} {# Etend le block head défini dans le layout  #}
    <meta property="og:title" content="{{ article.articleTitle }}" />
    {# <meta property="og:url" content="{{ app.request.uri }}" /> #}
    {# <meta property="og:image" content="{{ asset('path/to/article/image.jpg') }}" /> #}
    <meta property="og:type" content="article" />
{% endblock %}

{% block title %}{{article.articleTitle}}{% endblock %}

{% block page_title %} {{ article.articleTitle }} {% endblock %}

{% block body %}
{{ parent() }}{# Inclus le "page_title" ainsi que les propriétés du header #}

<main class="wrapper">
    {#--------------------------------------- Section article ----------------------------------------#}
    <article class="article_content">
        <span id="article_date">Rédigé par TuttoPasta, le {{ article.date }} | <a href="#comment_title">{{article.comments|length}} {% if article.comments|length <= 1 %}Commentaire{% else %}Commentaires{% endif %}</a></span>
        
        {% for category in article.categories %}
        <p>{{ category.categoryName }}</p>
        {% endfor %}
        <div class="article_description">
            {{ article.articleContent|raw }} <br>
        </div>
        <div class="article_tags">
            {% for tag in article.tags %}
                <span class="tag">{{ tag.tagName }}</span>
            {% endfor %}
        </div>        
        <hr class="separator">

        <div class="social_share">
            <p>Partager cet article : </p>
            <a href="https://www.facebook.com/sharer/sharer.php?u=http://127.0.0.1:8000/blog/{{article.slug}}" class="social_icons" target="_blank">
                <img src="{{ asset('/img/icons/facebook.svg')}}" alt="icône Facebook">
            </a>
            <a href="https://https://www.instagram.com/share/url/?url=http://127.0.0.1:8000/blog/{{article.slug}}" class="social_icons" target="_blank">
                <img src="{{ asset('/img/icons/instagram.svg')}}" alt="icône Instagram">
            </a>
            <a href="https://x.com/intent/post?url=http://127.0.0.1:8000/blog/{{article.slug}}" class="social_icons" target="_blank">
                <img src="{{ asset('/img/icons/twitter.svg')}}" alt="icône Twitter">
            </a>
            <a href="https://www.linkedin.com/shareArticle?mini=true&url=http://127.0.0.1:8000/blog/{{article.slug}}" class="social_icons" target="_blank">
                <img src="{{ asset('/img/icons/linkedin.svg')}}" alt="icône Linkedin">
            </a>
        </div>
        <hr class="separator">
        {#--------------------------------------- Section commentaires ----------------------------------------#}
        {# Compteur de commentaires #}
        <span id="comment_title" class="stickers_pink" data-count="{{article.comments|length}}">
            {{article.comments|length}}
            {% if article.comments|length <= 1 %} 
                Commentaire:
            {% else %} 
                Commentaires:
            {% endif %}
        </span>
        {# Contenu du commentaire #}
        <section id="comments_section">
            {% set sortedComments = article.comments|sort((a, b) => b.commentDate.timestamp - a.commentDate.timestamp) %}
            {% for comment in sortedComments %}
                <div class="comment" id="comment-{{ comment.id }}" data-slug="{{ article.slug }}">
                    <div class="comment_head">
                        {# <p>{{ comment.user.username }}</p> #}
                        {% if comment.user %}
                            {{ comment.user.username|capitalize }}
                        {% else %}
                            {{ comment.username }}
                        {% endif %}
                        <p>{{ comment.commentDate|date("d/m/Y à H:i") }}</p>
                    </div>
                    <div class="comment_content">
                        <p>{{ comment.commentContent }}</p>
                    </div>

                    {# Suppression ou édition si utilisateur connecté = auteur ou admin #}
                    {% if is_granted('ROLE_ADMIN') or (app.user and comment.username == app.user.username) %}
                    <div class="comment_actions">
                        <a href="#" class="edit_comment" data-id="{{ comment.id }}"><img src="{{ asset('/img/editer.png')}}" height="18" alt="icône d'édition"></a>
                        <a href="#" class="delete_comment" data-id="{{ comment.id }}"><img src="{{ asset('/img/annuler.png')}}" height="18" alt="icône d'annulation"></a>
                    </div>
                    {% endif %}
                </div>
            {% endfor %}
        </section>
        {# Ajout d'un commentaire #}
        {% if not app.user %}   
            <a id="comment_join_btn" class="full_button_black" href="{{ path('app_register') }}">Inscrit toi et joins la conversation</a>
        {% else %}
            <span>Laisser un commentaire</span>
            {{ form_start(form, {'action': path('app_article_addComment', {'slug': article.slug}), 'method': 'POST', 'attr': {'id': 'comment_form'}}) }}
                {{ form_row(form.commentContent) }} 
                <div class="error_msg" id="message_error"></div>

                {{ form_errors(form) }}
                <button id="saved_comment" class="full_button_black" type="submit">Envoyer</button>
            {{ form_end(form) }}
        {% endif %}
    </article>

    <hr class="separator">

    {#--------------------------------------- Section Autres articles ----------------------------------------#}
    <section>
        <ul class="article_list">
            {% for article in articles|slice(2) %}
            <li class="article_items">
                <a href="{{ path('app_article', { 'slug': article.slug }) }}">
                        <article class="article_card">
                            <h2 class="article_card_title">{{ article.articleTitle }}</h2>
                            <p>{{ article.date }}</p>
                            <div class="article_tags">
                                {% for tag in article.tags %}
                                    <span class="tag">{{ tag.tagName }}</span>
                                {% endfor %}
                            </div>    
                            <div class="see_more">Voir l'article <img src="{{ asset('/img/arrow_right.svg')}}" height="18" alt="icône d'annulation"></div>  
                        </article>
                    </a>
                </li>
            {% endfor %}
        </ul>
    </section>
</main>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('/js/blog.js')}}"></script>
    {% endblock %}
